<!--index.wxml-->
<view class="container pageBook">
  <label>
    <text>账本名称</text>
    <input type="digit" confirm-type="done" class="list-add-input" bindinput="inputValue" value="{{bookName}}"></input>
  </label>
  <button type="primary" bindtap="save">保存</button>
</view>
